---
description: Flutter 테스트를 실행하고 결과를 보고합니다. 전체 테스트 또는 특정 파일 테스트를 지원합니다.
---

# Test Workflow

## Step 1: 테스트 범위 확인
사용자에게 테스트 범위를 확인한다:
- 전체 테스트 (`flutter test`)
- 특정 파일/폴더 (`flutter test test/특정경로/`)

## Step 2: 테스트 실행
```bash
flutter test --reporter=expanded
```
// turbo

## Step 3: 결과 분석
테스트 결과를 분석하여 보고한다:

```markdown
## 🧪 테스트 결과

### 요약
| 항목 | 결과 |
|------|------|
| 총 테스트 | N개 |
| ✅ 통과 | N개 |
| ❌ 실패 | N개 |
| ⏭️ 스킵 | N개 |
| ⏱️ 소요 시간 | N초 |

### ❌ 실패한 테스트 (있는 경우)
```
파일: test/xxx_test.dart
테스트: "테스트 이름"
에러: 에러 메시지
```

### 💡 수정 제안
- [실패 원인 분석 및 수정 방법]
```

## Step 4: 커버리지 (선택)
사용자가 원하면 커버리지 리포트 생성:
```bash
flutter test --coverage
```
// turbo

```bash
# 커버리지 HTML 리포트 생성 (lcov 필요)
genhtml coverage/lcov.info -o coverage/html || echo "genhtml not installed"
```

## Step 5: 다음 단계 안내
```markdown
### 다음 단계
- 모든 테스트 통과 → `/commit`으로 커밋
- 테스트 실패 → 코드 수정 후 다시 `/test`
- 커버리지 확인 → `open coverage/html/index.html`
```
